[project]
name = "kokoro-local-tts-backend"
version = "0.1.0"
description = "Personal local FastAPI wrapper around Kokoro ONNX TTS"
requires-python = ">=3.10,<3.14"
dependencies = [
    "fastapi>=0.115.0",
    "kokoro-onnx==0.5.0",
    "pydantic>=2.8.0",
    "soundfile>=0.12.1",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
